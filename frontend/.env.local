# Local development — points the frontend at the local backend.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase login left blank on purpose → the app runs "open" (no login wall) for local dev.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
